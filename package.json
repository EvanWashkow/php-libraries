{
  "private": true,
  "scripts": {
    "postinstall": "husky install && cp -a .husky/. .git/hooks/"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.0.3",
    "@commitlint/config-conventional": "^17.0.3",
    "@semantic-release/gitlab": "^10.1.3",
    "husky": "^8.0.1"
  }
}
