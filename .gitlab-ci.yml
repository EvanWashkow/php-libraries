stages:
  - build
  - test

build-job:
  stage: build
  script:
    - docker build -t env:latest .

unit-test-job:
  stage: test
  script:
    - echo "Running unit tests... This will take about 60 seconds."
    - sleep 60
    - echo "Code coverage is 90%"
