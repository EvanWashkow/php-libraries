<?php
declare(strict_types=1);

namespace PHP\Enums;

/**
 * Defines a strict set of bit flags, allowing the user to specify one or more
 * of them in a bitwise OR (|) operation.
 */
abstract class BitEnum extends IntegerEnum
{
    
}
